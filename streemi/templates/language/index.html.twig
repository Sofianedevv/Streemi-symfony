{% extends 'base.html.twig' %}

{% block title %}Langue{% endblock %}

{% block content %}
    <h1 class="text-2xl font-semibold mb-6">Langue</h1>

    <div class="overflow-x-auto bg-white shadow-md rounded-lg">
        <table class="min-w-full table-auto">
            <thead class="bg-gray-100">
                <tr>
                    <th class="px-6 py-3 text-left text-sm font-medium text-gray-500 uppercase">Id</th>
                    <th class="px-6 py-3 text-left text-sm font-medium text-gray-500 uppercase">Name</th>
                    <th class="px-6 py-3 text-left text-sm font-medium text-gray-500 uppercase">Code</th>
                    <th class="px-6 py-3 text-left text-sm font-medium text-gray-500 uppercase">Actions</th>
                </tr>
            </thead>
            <tbody class="text-gray-700">
                {% for language in languages %}
                    <tr class="border-b hover:bg-gray-50">
                        <td class="px-6 py-4 text-sm">{{ language.id }}</td>
                        <td class="px-6 py-4 text-sm">{{ language.name }}</td>
                        <td class="px-6 py-4 text-sm">{{ language.code }}</td>
                        <td class="px-6 py-4 text-sm">
                        <a href="{{ path('app_language_edit', {'id': language.id}) }}" class="text-yellow-600 hover:text-yellow-800">Edit</a>
                            <span class="mx-2">|</span>
                            <a href="{{ path('app_language_delete', {'id': language.id}) }}" class="text-blue-600 hover:text-red-800">{{ include('language/_delete_form.html.twig') }}</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="px-6 py-4 text-center text-gray-500">No records found</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <a href="{{ path('app_language_new') }}" class="mt-4 inline-block bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">Create New</a>
{% endblock %}
